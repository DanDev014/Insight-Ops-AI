# This engine's Neon connection string (or it falls back to DATABASE_URL).
PROJECTS_DATABASE_URL="postgresql://neondb_owner:PASSWORD@HOST.neon.tech/neondb?sslmode=require"
# Set "true" to run on generated sample data instead of the live DB.
USE_SAMPLE_DATA="false"
